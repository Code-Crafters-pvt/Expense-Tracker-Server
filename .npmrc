# Enforce exact versions - no automatic updates

save-exact=true

save-prefix=''

package-lock=true



# Prevent automatic upgrades when installing new packages

# This handles peer dependency conflicts without upgrading existing packages

legacy-peer-deps=true

